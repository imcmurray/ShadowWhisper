{
  "mcpServers": {
    "playwright": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-p", "3000:3000",
        "mcr.microsoft.com/playwright/mcp",
        "--executable-path", "/ms-playwright/chromium-1202/chrome-linux64/chrome",
        "--headless",
        "--no-sandbox"
      ]
    }
  }
}
